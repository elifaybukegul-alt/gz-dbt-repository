version: 2

sources:
  - name: raw  # SQL'deki source('raw', ...) kısmındaki ilk parametre
    database: veritabanı_adın  # Kullandığın DB adı (Örn: analytics_db)
    schema: raw_schema_adın    # Verilerin gerçekte durduğu şema adı
    tables:
      - name: product
      - name: sales
      - name: ship 